name: Periodic health check

on:
  schedule:
    - cron: "0 0 * * 1" # once a week (Monday 00:00 UTC)
  workflow_dispatch:

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: |
          npm ci
          npm run test